type Room {
  id: ID!
  users: [User!]
}

type Query {
  room(roomId: String!): Room!
  rooms: [Room!]
}

type Mutation {
  createRoom: Room!
  sendMessage(userId: String!, message: String!): Response!
}

type Subscription {
  roomUsers(roomId: String!): User!
}

type Room {
  id: ID!
  users: [User!]
}

type Query {
  rooms: [Room!]
}

type Mutation {
  createRoom: Room!
  sendMessage(userId: String!, message: String!): Response!
}

type Subscription {
  roomUsers(roomId: String!): Room!
}
